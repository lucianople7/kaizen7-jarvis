{
  "name": "list_idea_groups",
  "description": "Lists all idea groups (columns) in the ideas board for a Buffer organization. Idea groups are used to organize ideas into named columns. Returns each group's id, name, and lock status. Call get_account first to obtain your organization ID.",
  "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {
      "organizationId": {
        "type": "string",
        "pattern": "^[a-f\\d]{24}$",
        "description": "The ID of the organization to list idea groups for. Use get_account to retrieve your organization IDs."
      }
    },
    "required": [
      "organizationId"
    ]
  }
}