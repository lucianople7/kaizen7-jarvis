{
  "name": "list_channels",
  "description": "Lists all social media channels connected to a Buffer organization. Returns summary info (id, name, displayName, service, type, avatar, connection status). Use get_channel for detailed information about a specific channel, including posting schedule. Call get_account first to obtain your organization ID.",
  "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {
      "organizationId": {
        "type": "string",
        "pattern": "^[a-f\\d]{24}$",
        "description": "The ID of the organization to list channels for. Use get_account to retrieve your organization IDs. Tell the user which organization (by name) you are querying."
      }
    },
    "required": [
      "organizationId"
    ]
  }
}