{
  "name": "execute_mutation",
  "description": "Executes a GraphQL mutation to modify data in Buffer. PREREQUISITE: Call introspect_schema first to learn available mutations and fields. Use this only for operations not covered by domain tools. Do not guess mutation or field names — use only names from the schema.",
  "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {
      "summary": {
        "type": "string",
        "minLength": 1,
        "description": "A brief, non-technical description of what this operation does and why. This is shown to users in permission prompts to help them understand the action."
      },
      "mutation": {
        "type": "string",
        "description": "The GraphQL mutation to execute. Field and type names MUST match the schema returned by introspect_schema."
      },
      "variables": {
        "description": "Variables for the mutation as a JSON object.",
        "type": "object",
        "propertyNames": {
          "type": "string"
        },
        "additionalProperties": {}
      },
      "operationName": {
        "description": "Name of the specific mutation to run if multiple are defined.",
        "type": "string"
      }
    },
    "required": [
      "summary",
      "mutation"
    ]
  }
}