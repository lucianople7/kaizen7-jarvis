{
  "name": "find_backlinks",
  "description": "Return every memory whose body contains `[[<title>]]` pointing at the target. Useful before deleting or renaming a memory.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "target": {
        "type": "string",
        "minLength": 1,
        "description": "The case-insensitive H1 title being linked to."
      },
      "cwd": {
        "type": "string",
        "description": "Optional. Absolute path used to discover the hub by walking up for `.bridgememory/`. Defaults to the server's process cwd."
      }
    },
    "required": [
      "target"
    ],
    "additionalProperties": false,
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}