# Distribution denylist — paths that must NEVER ship to the public repo.
#
# This is a CURATED DENYLIST, not a working-vs-dist diff. New feature code is
# never listed here, so it ships automatically; only personal / secret /
# internal-process artifacts are withheld. Each line is a gitignore-ish glob
# matched against the posix, repo-relative path of every git-TRACKED file.
# Globs: `**` = any depth, `*` = within a path segment, `?` = one char.
# Lines starting with `#` and blank lines are ignored.
#
# Decision record: the maintainer chose (2026-05-31) to keep withholding all
# internal dev docs (an openclaw-style clean public repo) and the encrypted
# release-signing keys (forks generate their own). Confirmed in the skill plan.
#
# WHY a denylist and not the file diff: the working↔dist diff conflates two
# different things — deliberately-withheld files AND files merely newer than the
# last release. Baking the diff into "always exclude" would freeze out real
# features (e.g. jarvis/state/*, jarvis/plugins/tts/fallback_tts.py). So those
# are intentionally ABSENT from this list and ship normally.

# --- .claude/ contributor toolchain: SHIP it (like the openclaw repo), minus the
#     maintainer's own release skill (2026-06-27, revised) ----------------------
# REVISED: .claude/ is Claude-Code CONTRIBUTOR tooling — subagent definitions,
# slash-commands, dev skills — exactly what a fork needs to keep developing with
# the same AI workflow. The reference project openclaw/openclaw (380k+ stars)
# ships .agents/ + AGENTS.md + CLAUDE.md publicly; this is a professionalism
# signal, not clutter. NOTHING in the runtime jarvis/ package loads it, so it is
# product-safe. (The single personal path in phase6-adr-update/SKILL.md is
# depersonalized by the PII scrub, not withheld.)
#
# The privacy gate itself is personal: scripts/ci/privacy_gate/references/
# (pii-scrub.tsv, secret-allowlist.tsv) carry the maintainer's real name +
# emails as scrub *patterns* — shipping them would leak exactly what they exist
# to remove, and a fork generates its own. So withhold that whole subtree.
scripts/ci/privacy_gate/**

# --- .mailmap: shipped again since 2026-07-28 (maintainer directive) ---------
# The 2026-06-01 full-depersonalization entry withheld .mailmap because it then
# carried the maintainer's two private e-mail addresses as file data. That era
# is retired (2026-07-17: one shared git history) and its premise no longer
# holds either: the public history is NOT purely noreply-authored (generic
# "Personal Jarvis Maintainer" and placeholder identities exist there), so the
# file IS load-bearing for honest `git shortlog` attribution. The tracked
# .mailmap now must never contain a real personal address (see
# scrub-exempt.txt), which removes the reason to withhold it.

# --- Encrypted release-signing keys (maintainer identity) -------------------
install/keys/*.key.enc

# --- Wave-2 demo passphrase carriers (2026-06-17) ---------------------------
# The encrypted key above is withheld, but the passphrase that decrypts it is
# committed in plain text in install/TRUST_ROOT.md (§3.3) and in these two
# ceremony docs (the `WAVE2_CEREMONY_PASSPHRASE=...` / `-pass "pass:..."`
# lines). Withhold the passphrase too so a public release never ships either
# half of the offline-ceremony key. (Private repo keeps both — Foundation-demo
# posture, rotation deferred to the documented v0.5.0 cut.)
install/TRUST_ROOT.md
docs/supply-chain/wave2-key-ceremony.md
docs/supply-chain/wave4-distribution.md

# --- Internal scratch scripts (underscore-prefixed convention) --------------
scripts/_*.py

# --- Dead WhisperBar dev-probe scripts (2026-06-30) -------------------------
# Standalone desktop-capture probes that still `import jarvis.ui.whisperbar`,
# a module renamed to jarvis.ui.jarvisbar (whisperbar->jarvisbar rename,
# 2026-06-30). The import is dead, so the scripts are broken dev-only probes
# with no downstream value — and the release verify gate (top-level unresolved
# import) correctly refuses to ship them. They are not product code; a fork has
# no use for them.
scripts/whisperbar_live_probe.py
scripts/whisperbar_thinking_preview.py

# --- Personal operational state + scratch (2026-06-19 privacy review) -------
# config-soll.json is the maintainer's PRIVATE drift-guard desired-state pin  # i18n-allow
# (internal _reason changelog re: GCP/billing/model mandates) — a fork generates
# its own. .tmp_research/ is scratch probing. Neither has downstream value.
# drift-guard-task.xml is a generated Windows Task-Scheduler export carrying the
# maintainer's Windows account SID (S-1-5-21-...-500) + local paths — a fork
# regenerates it from the .ps1; it must never ship (2026-06-19 scan BLOCK).
scripts/config-soll.json
scripts/drift-guard-task.xml
.tmp_research/**
# Root-level optimization/loop ledgers — internal dev scratchpads (the running
# notes of a /loop session: measurements, reasoning, file paths). Like the
# untracked progress.md, they have no downstream product value. boot-progress.md
# was committed (so it is tracked) and would otherwise ship; withhold it.
boot-progress.md
# Root-level PDF: an internal German bug report with desktop screenshots the
# maintainer keeps as a reference for another agent session (memory: intentional
# in the WORKING repo). Screenshots can carry personal data and the binary is
# opaque to the text scanners — it must never ship (2026-07-07 sub-agent
# privacy-review finding). Glob covers the "(2)" suffix and any sibling copies.
Unbenanntes*.pdf
*.pdf
# Same class as the PDF above but the Markdown siblings ("Unbenanntes Dokument
# (7).md" / "(8).md"): internal German bug-report / handoff notes to another
# agent session, tracked by accident (auto-save hook). They carry German prose +
# wake-word/pipeline logs and have no downstream product value (2026-07-08 scan
# BLOCK). Withhold every such root-level scratch doc at any extension.
Unbenanntes*
# Internal WSL verification scratch (2026-07-07 sub-agent privacy review):
# carries the maintainer's personal home path + a Claude session UUID and
# references non-shipped data/ captures. Dev scratch, no downstream value.
.tmp_pytest/**
# README wiki screenshot (2026-07-07 sub-agent privacy review): the capture
# showed the maintainer's LIVE personal vault (relocation plans, finances,
# contacts). Withheld until a staged demo-vault screenshot replaces it; the
# README image reference was removed in the same review.
assets/screenshots/view-wiki.png
# Personalized visual QA/reference captures (2026-08-03 privacy review): these
# contain a configured person's name, wake phrase, profile filename, or other
# live-looking account context. They are review inputs, not runtime assets.
assets/screenshots/app-home.png
assets/screenshots/app-desktop.png
qa/ref/**

# --- Internal dev-process docs: whole subtrees ------------------------------
docs/superpowers/**
docs/plans/**
docs/phase-b1-wiki-curator/**
docs/diagnostics/**

# --- Contributor doctrine now SHIPS (2026-06-27, revised) -------------------
# CLAUDE.md + CLOUD.md are the binding contributor doctrine. AGENTS.md is the
# byte-twin of CLAUDE.md and ALREADY ships, so withholding CLAUDE.md was
# inconsistent — its content was public via AGENTS.md regardless. the openclaw repo ships
# CLAUDE.md + VISION.md publicly; matching that is the professional move. Both
# are depersonalized by the PII scrub on the way out. (docs/BUGS.md stays
# withheld below — that is the internal bug log, not contributor doctrine.)

# --- BUGS.md now SHIPS (2026-06-27): the recurring-bug "journey" log is strong
# --- contributor/reader material. VERIFIED — applying the PII scrub to it leaves
# --- 0 personal paths (5 -> <USER_HOME>), 0 real name, 0 private mail; only
# --- 127.0.0.1 (loopback) + hardware names (RTX / headset) remain, which are
# --- non-identifying. The fail-closed scrub+scan re-checks on every ship. -----

# --- Internal dev-process docs: individual files ----------------------------
docs/adr/0009-self-healing-worker-critic.md
docs/agents-self-verification.md
docs/deepgram-stt-research-2026-05-20.md
docs/jarvis-agents-bridge.md
docs/jarvis-agents-spawn-failure-analysis-2026-05-18.md
docs/persona-audit-report.md
docs/persona-refactor-results.md
docs/persona-research.md
docs/phase1c-test-results.md
docs/phase2-integration-test-results.md
docs/phase6-handoff-2026-04-26.md
docs/phase6-test-report.md
docs/spike-results-jarvis-agents.md
docs/voice-acceptance-brief.md
docs/bug-019-spawn-failure-report.html
docs/jarvis-board/PHASE_A_DONE.md
docs/phase5-research.md
docs/phase5-integration-test-results.md

# --- Personalized demo capture (2026-08-03 privacy review) ------------------
# The recording, GIF, poster, and render manifest carry a real account email,
# machine identifier, and private session-history text. Keep the independently
# sanitized Agentic IDE demo, but withhold this capture family until rerendered
# entirely from synthetic data.
assets/demo/personal-jarvis-demo-poster.png
assets/demo/personal-jarvis-demo.gif
assets/demo/personal-jarvis-demo.manifest.json
assets/demo/personal-jarvis-demo.mp4

# --- The whole video/ Remotion build project (2026-06-27) -------------------
# video/ is the Remotion source project used to RENDER the YouTube onboarding
# film; it is a generator tool, not a runtime asset. The frontend embeds the
# finished film straight from youtube-nocookie.com (IntroVideoScreen.tsx) and
# loads nothing from this dir; .dockerignore already excludes video/. This
# subsumes the earlier video/analysis/** entry: those frames are review/debug
# artifacts that can carry personalized demo text in pixels (caught by semantic
# image scanners, not file-text scanners), so the whole tree stays out.
video/**

# --- Standalone side-projects: ship in their OWN repos, not the flagship (2026-06-27) ---
# Self-contained projects that merely co-live in this monorepo. The runtime
# jarvis/ package imports NOTHING from them (verified 2026-06-27 — 0 import
# sites). Patterns are ^-anchored, so each hits ONLY the top-level dir, never a
# same-named subdir (e.g. src/** does not touch jarvis/ui/web/frontend/src/).
#   - skillbook/ : independent decentralized agent-memory research project
#                  (own CLAUDE.md / ADRs / tests; unrelated to the product)
#   - keyproxy/  : standalone LLM-key reverse proxy ("zero dependency on jarvis")
#   - src/       : orphaned duplicate of the web-search skill; the LIVE tool is
#                  jarvis/plugins/tool/search_web.py (no sys.path/loader points here)
# DELIBERATELY KEPT (verified load-bearing, do NOT add): board-backend/
# (jarvis/board/sync.py + federation_proxy_routes.py import board_backend.crypto),
# ui/ (the Orb overlay is imported at runtime), conductor/ (3 import sites).
# (OS-Level/ was removed from the repo in 2026-07 — the CU screen indicator
# in jarvis/cu/indicator replaced its edge-glow.)
skillbook/**
keyproxy/**
src/**

# --- Internal maintainer demo / perf scripts (2026-06-27) -------------------
# tools/ holds board_demo.py / board_pentest.py / board_perf.py — internal
# bring-up & perf probes for the Board, not product code. A fork has no use for
# them (the product's own tooling lives inside the jarvis/ package).
tools/**

# --- Internal QA protocols (2026-06-27) -------------------------------------
# docs/qa/ is the maintainer's internal voice-QA checklist — same internal-
# process class as docs/superpowers/** / docs/plans/** already withheld above.
docs/qa/**

# --- Internal privacy/red-team agent spec (2026-06-30) ----------------------
# docs-privacy-reviewer.md is the maintainer's OWN depersonalization red-team
# guide: in source it enumerates the real maintainer identity (the
# `<MAINTAINER_NAME>` and `<ALTERNATE_NAME>` variants), the private
# email shapes, GitHub login) and the entire scrub scheme. Shipping the doc that
# lists what to hide is itself the leak (2026-06-30 sub-agent review). It is an
# internal dev tool with no downstream value; a fork writes its own.
.claude/agents/docs-privacy-reviewer.md
.agents/agents/docs-privacy-reviewer.md
.codex/agents/docs-privacy-reviewer.toml

# Live-session forensic evidence contains private session identifiers and
# identity-bearing transcript excerpts. Keep the diagnosis local; public bug
# documentation must use synthetic reproductions instead.
docs/reports/realtime-double-greeting-and-stall-deep-dive-2026-07-17.md
docs/phantom-worker-aborted-deep-dive/README.md

# The local bug register contains raw live-session forensics newer than the
# reviewed public baseline. Preserve that public baseline until each new entry
# has a synthetic reproduction.
docs/BUGS.md

# --- Internal handoff notes carrying the maintainer's private email (2026-06-30) ---
# docs/handoffs/ holds internal review-answer handoffs. report-tutorial-review-
# answers.md is a meta-note ABOUT the depersonalization scheme: it quotes the
# maintainer's private email in plain text (lines 30/142/242) as a pattern to
# protect — same self-leak class as docs-privacy-reviewer.md and the pii-scrub
# map already withheld above. The email is a block-only scrub rule, so leaving
# the file in would fail-close the ship anyway; withhold the whole subtree so a
# valid snapshot never depends on a last-minute abort. Internal process doc with
# no downstream value; a fork writes its own.
docs/handoffs/**

# --- Voice-session transcripts (2026-06-30) ---------------------------------
# voice-session-*.{md,txt} are raw transcripts of the maintainer talking to
# Jarvis (e.g. "was-geht-ab…") — personal speech content with no downstream
# value, occasionally git-tracked by accident (an auto-save / `git add -A` in a
# parallel session). Withhold every voice-session transcript, at any depth, so a
# stray one never reaches public history.
voice-session-*
**/voice-session-*

# --- Internal pick-up / handoff prompt notes (2026-06-30) -------------------
# reporttutorialpickupprompt*.md is a loose top-level "pick-up" prompt: an
# internal instruction to a Claude session to run the §2 privacy review. It
# names concrete maintainer-identity variants and enumerates the
# private categories to hunt for — the same self-leak class as
# docs-privacy-reviewer.md and docs/handoffs/** already withheld above (shipping
# the doc that lists what to hide IS the leak). Internal process artifact, zero
# downstream value, occasionally git-tracked by accident. Withhold any such loose
# pick-up/handoff prompt at the repo root.
reporttutorialpickupprompt*
**/reporttutorialpickupprompt*
*pickupprompt*.md

# --- Launch-marketing drafts (2026-07-02) ------------------------------------
# marketing/launch/* holds the maintainer's pre-launch drafts (Show-HN post,
# Reddit/X threads, hero one-liners, a manual GitHub-setup checklist). Internal
# process artifacts: publishing the launch plan before the launch IS the leak,
# and none of it is product content a downstream user needs. A fork writes its
# own marketing. Withhold the whole subtree.
marketing/**

# --- Marketing-readiness audit (2026-08-06) ----------------------------------
# docs/reports/personal-jarvis-marketing-readiness-* is a pre-launch readiness
# audit of the maintainer's own go-to-market plan, not product documentation.
# Same category as marketing/** above and withheld for the same reason, plus it
# is ~490 KB of generated report artifact every clone would have to carry.
docs/reports/*marketing-readiness*

# --- Unfinished TTS-eval harness (2026-07-08) --------------------------------
# jarvis/speech/tts_eval/* is an internal, in-progress TTS quality-eval tool
# (corpus + metrics + harness), committed by a parallel session's auto-save. It
# is not user-facing (nothing in jarvis/ imports it), and its German test-corpus
# sentences are not yet on the English-artifact allowlist. Slated for the 1.0.4
# cut once finished + translated/allowlisted. Withhold the module and its tests
# from the 1.0.3 release.
jarvis/speech/tts_eval/**
**/tts_eval/**
tests/unit/speech/tts_eval/**

# --- Obsolete internal Realtime design notes (2026-07-13) -------------------
# These phase-0/1 execution notes still describe Realtime as default-off and
# include an obsolete agent commit recipe. Withhold them until they are replaced
# by accurate user-facing architecture documentation. The finished engine and
# its tests are intentionally public.
docs/realtime-voice/**

# --- Maintainer-private agent tooling + internal handoffs (2026-07-13, v1.0.6
# privacy-audit findings) ------------------------------------------------------
# .codex/hooks.json wires SessionStart/UserPromptSubmit/Stop hooks to the
# maintainer's private BridgeSpace binary (bs-claude-hook.cjs) that no
# downloader has — environment-specific tooling, not project knowledge. The
# .codex/agents/*.toml mirror stays public (agent definitions are addressed to
# every coding agent, like .claude/agents).
.codex/hooks.json
# .codex/visualizations/** carries rendered analyses of the maintainer's own
# VOICE SESSIONS — literal private utterances (v1.0.7 gate catch, BUG class:
# a parallel session tracked the folder). Session-derived, never product.
.codex/visualizations/**
# Root-level Codex dev-handoff task prompts are internal working notes, not
# user-facing docs.
codex-*.md

# --- Tests for withheld maintainer tooling (2026-07-14, v1.0.6 CI forensic) ---
# These test the privacy gate and the drift-guard config-soll pin — both of
# which are themselves denylisted above, so the tests can only fail in the
# public snapshot (FileNotFoundError on the withheld files).
tests/unit/ci/test_privacy_pre_push.py
tests/unit/scripts/test_drift_guard_brain_primary_contract.py
