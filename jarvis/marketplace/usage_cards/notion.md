---
plugin_id: notion
keywords: notion, seite, page, notiz, note, notizen, notes, doc, docs, wiki, datenbank, workspace, suche, search, block, datenbank, database, kommentar, comment, erstellen, create
---
Use notion/* tools to read, create, and update Notion pages, databases, and comments.
- Search across the workspace with a keyword before creating new content.
- When writing to a database, retrieve the schema first to match property types.
- Create and update pages directly (full autonomy); report the page title and URL afterwards.
- Notion's MCP scope covers only content the connected integration can access.
