# Managed local-realtime server: the tested, version-pinned core set
# (AD-2 of docs/plans/local-realtime-one-click-install-plan.md).
# torch/torchaudio are installed separately by the engine because their
# flavor (CUDA index vs default) depends on the probed hardware.
speech-to-speech==0.2.12
faster-qwen3-tts==0.3.2
nano_parakeet==0.2.1
qwen-tts==0.1.1
pocket-tts==2.1.0
onnxruntime==1.28.0
