# Skillbook Changelog

One line per meaningful unit. Newest at top. Failed approaches recorded with reason so future sessions do not repeat dead ends.

- 2026-05-26 chore(branch-hygiene): branch cleanup — refactor/extract-fakes contained a parallel-session alien commit 8edbe9051 (jarvis/ grace-on-COMPLETE) interleaved with the 6 fakes-extraction commits. Cherry-picked the 6 onto refactor/extract-fakes-clean off a fresh origin/main, fast-forwarded main, preserved the alien commit object on feat/grace-on-complete, deleted the dirty refactor/extract-fakes. main now contains atomic fakes commits in chronological order (69a4e76b6..e7cdefeca); 74 tests green; SV-1..SV-7 all pass.
- 2026-05-26 test(enforcement): tests/test_no_mocks_in_production.py greps src/skillbook/ for `class (Mock|Fake|Stub|Dummy|InProcess)\w*` and for `from tests`/`import tests`/`from skillbook.tests` — both must return zero hits. Sanitized literal `FakeLLM()`/`MockSymcon`/`FakeSymcon` mentions out of src/ docstrings + hints so the strict SV-5 grep also returns clean. 74 green.
- 2026-05-26 refactor(agent): tighten AgentInstance.build to require llm + actors + transport as keyword args (no None-defaults). attach_transport() removed; SyncEngine is wired inside build. MissingAdapterError raised on any nil adapter. Capstone passes actors=[] + transport=t explicitly. 72 green.
- 2026-05-26 refactor(p2p-sync): extract InProcessTransport -> tests/fakes/transport.py (name retained — "InProcess" is descriptively accurate). src/skillbook/p2p_sync/transport.py keeps only the Transport Protocol. test_transport + test_engine + test_capstone import from tests.fakes.transport. 72 green.
- 2026-05-26 refactor(symcon-bridge): extract MockSymconActor -> tests/fakes/symcon.py:FakeSymconActor. src/skillbook/symcon_bridge/actor.py keeps only SymconActor (real JSON-RPC). test_actor + test_capstone import FakeSymconActor. 72 green.
- 2026-05-26 refactor(ace-core): extract MockLLM -> tests/fakes/llm.py:FakeLLM. src/skillbook/ace_core/llm.py keeps only LLM Protocol + AnthropicLLM; default_llm() now raises MissingAdapterError instead of falling back. agent.py: llm is required (no None fallback). test_capstone + test_reflector import FakeLLM. 72 green.
- 2026-05-26 refactor(foundation): MissingAdapterError in src/skillbook/errors.py; ADR-0010 explains the test-double extraction motivated by self-audit verdict (C); pyproject `norecursedirs = ["tests/fakes"]`; empty `tests/fakes/__init__.py`. No fakes moved yet; 72 tests still green.
- 2026-05-26 capstone: AgentInstance composes memory/generator/reflector/curator/sync; tests/test_capstone.py runs the full 7-step ACE closed loop + idempotent sync regression under --seeds=5. Full suite 72 green (12 modules + capstone, 10 capstone runs).
- 2026-05-26 symcon-bridge: JsonRpcClient (stdlib urllib via asyncio.to_thread, injectable http_post for tests), SymconActor (wraps client + hard TimeoutError), MockSymconActor (programmable failures_until_ok). 7 unit tests green.
- 2026-05-26 p2p-sync: BloomFilter (k=4, SHA-256-derived hash functions, FPR < 5% at n=200/m=8192), crdt_merge OR-Set semantics (tombstone wins, idempotent, commutative), InProcessTransport.pair() with AP-18 subscriber-isolation, SyncEngine full-rule push protocol + own-loopback guard. 19 unit tests green, full suite 62 under --seeds=5.
- 2026-05-26 ace-core: Generator (skillbook-driven retry strategy + per-attempt trace recording), Curator (embedding-similarity dedup, OR-Set-safe inserts), MockLLM/AnthropicLLM (ADR-0004), RecursiveReflector (subprocess REPL sandbox per ADR-0007, -I isolated mode, hard timeout). 15 unit tests green, full suite 36 under --seeds=5.
- 2026-05-26 guardrails: AgentDoG (Source/FailureMode/Consequence StrEnums, ADR-0008), Diagnostic pydantic with suggested_rule field, LATSEngine pre-emptive rollback + CircuitBreaker. 12 unit tests green.
- 2026-05-26 memory-layer: Rule/TraceStep/Entity/Relation pydantic models, SQLiteMemoryStore with skb_ prefix (DoD-4), JSON1 indexed actor lookup, OR-Set tombstone column, HashEmbedder (384-dim deterministic). 9 unit tests green.
- 2026-05-26 phase-0 scaffolding: src/ layout, pyproject (uv), CLAUDE.md (DoD-1..5), 9 ADRs (toolchain/SQLite/embeddings/LLM/MQTT/P2P/sandbox/taxonomy/deviations), conftest with --seeds option
